apiVersion: v1
kind: Service
metadata:
  name: {{ include "vote-demo-web.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "vote-demo-web.name" . }}
    helm.sh/chart: {{ include "vote-demo-web.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "vote-demo-web.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
